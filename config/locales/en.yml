en:
  locale_name: 'English'

  # ===========================================================================
  # = Users                                                                   =
  # ===========================================================================
  accounts:
    new:
      title: 'Sign Up'
    show:
      title: 'Welcome'

  messages:
    new:
      submit: 'Send'
      title: 'New Message'

  payments:
    new:
      submit: 'Buy Credit'
      title: 'New Payment'

  subscribers:
    create:
      thank_you: 'Thank You' # FIXME
    form:
      blurb: 'Find out when we launch--and maybe get a beta invite?'
      submit: 'Subscribe'
      title: 'Subscribe'

  user_sessions:
    new:
      required: 'Please sign in first.'
      title: 'Sign In'
    destroy:
      title: 'Sign Out'

  # ===========================================================================
  # = Administrators                                                          =
  # ===========================================================================
  admin:
    administrator_sessions:
      new:
        required: 'Please sign in first.'
        title: 'Sign In'
      destroy:
        title: 'Sign Out'
    pages:
      index:
        title: 'Welcome'
    subscribers:
      index:
        title: 'Subscribers'
      subscriber:
        destroy:
          confirm: 'Are you sure you want to unsubscribe {{email}}?'
          submit: 'Unsubscribe'
        invite:
          confirm: 'Are you sure you want to invite {{email}}?'
          count:
            one: '{{count}} Invitation'
            other: '{{count}} Invitations'
          submit: 'Invite'

  # ===========================================================================
  # = ActionMailer                                                            =
  # ===========================================================================
  mailer:
    invitation:
      created:
        subject: 'Your Wezesha Invitation Code' # FIXME
        body: 'Follow this link to create your Account.' # FIXME

  # ===========================================================================
  # = ActiveRecord                                                            =
  # ===========================================================================
  activerecord:
    errors:
      messages:
        equality: "must equal {{expected_value}}"

      models:
        account:
          attributes:
            subdomain:
              invalid: "may only contain lowercase letters"

  # ===========================================================================
  # = Authlogic                                                               =
  # ===========================================================================
  authlogic:
    error_messages:
      login_blank: "can't be blank"
      # login_not_found: does not exist
      # login_invalid: should use only letters, numbers, spaces, and .-_@ please.
      email_invalid: "should look like an email address"
      password_blank: "can't be blank"
      password_invalid: is invalid
      # not_active: Your account is not active
      # not_confirmed: Your account is not confirmed
      # not_approved: Your account is not approved
      # blank_record: You can not login with a blank record
      # new_record: You can not login with a new record
